<%= form_with(model: covenant, local: true) do |form| %>
  <% if covenant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(covenant.errors.count, "error") %> prohibited this covenant from being saved:</h2>

      <ul>
      <% covenant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :urlMultimedia %>
    <%= form.text_field :urlMultimedia, id: :covenant_urlMultimedia %>
  </div>

  <div class="field">
    <%= form.label :discipleship_id %>
    <%= form.text_field :discipleship_id, id: :covenant_discipleship_id %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id, id: :covenant_student_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
